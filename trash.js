// Assignment Code

// IF a user clicks the generate button 
// THEN prompted for character types;
// THEN the prompted for character length;

/*
var uppercase = "Do you want to use uppercase letters?";
var lowercase = "Do you want to use lowercase letters?";
var numbers = "Do you want to use numbers?";
var specialChar = "Do you want to use special characters?";
*/

var generateBtn = document.querySelector("#generate");

var charCount = window.prompt ("How many characters?");

generateBtn.onclick function () {
  return (answer);
}

/*
var uppercase = confirm(Do you want to use uppercase letters?");
var lowercase = confirm();
var numbers = confirm();
var specialChar = confirm();
var charCount = prompt();
*/

// create an array of the character type
// determine which boxes in the array are selected
// IF selected checkboxes is 0 
// AND the generate button is pressed
// THEN create alert, "You must select at least one character type."
// ELSE continue to generate password 




/*
// Write password to the #password input
function writePassword() {
  var password = generatePassword();
  var passwordText = document.querySelector("#password");

  passwordText.value = password;

}
// Add event listener to generate button
generateBtn.addEventListener("click", writePassword);
*/


//Make sure that you make your prompt equal to a variable so that you will have access to the value that the user inputs. Same for the confirms, they will return true or false

////var passwordCard = document.getElementById("password-card");
//var characterCard = document.getElementById("character-card");

//passwordCard.style.display = "none";
//characterCard.setAttribute("style", "display: inline;");